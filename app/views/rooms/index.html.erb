<h3>登録済み一覧</h3>
 
 <table>
   <thead>
     <tr>
       <th>画像</th>
       <th>施設名</th>
       <th>紹介文</th>
       <th>料金</th>
       <th>作成日</th>
     </tr>
   </thead>
 
   <tbody>
     <% @rooms.each do |room| %>
       <tr>
        <% if room.image?%>
         <td><%= image_tag room.image.url, size: "100x70" ,alt: "アイコン"%></td>
          <% else %>
          <td><%= image_tag ("noimage.png"), size: "100x70", alt: "アイコン"%></td>
          <% end %>
         <td><%= link_to room.name, room %></td>
         <td><%= room.introduction %></td>
         <td>¥<%= room.price %>~</td>
         <td><%= room.created_at.to_s(:datetime_jp2) %></td>
       </tr>
     <% end %>
   </tbody>
 </table>